import SwiftUI
import TokenDeskFeatures

struct ContentView: View {
    var body: some View {
        TokenDeskAppShell()
    }
}

#Preview {
    ContentView()
        .frame(width: 1280, height: 720)
}
